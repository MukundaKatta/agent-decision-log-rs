[package]
name = "agent-decision-log"
version = "0.1.0"
edition = "2021"
description = "WHY-layer decision log for AI agents — records options, chosen branch, rationale, and outcome"
license = "MIT"
repository = "https://github.com/MukundaKatta/agent-decision-log-rs"
keywords = ["agent", "decision", "audit", "llm", "observability"]
categories = ["api-bindings"]
exclude = ["target/*", ".github/*", "*.log"]

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[dev-dependencies]
tempfile = "3"
